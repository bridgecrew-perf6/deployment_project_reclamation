import { CustomDirecDirective } from './custom-direc.directive';

describe('CustomDirecDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomDirecDirective();
    expect(directive).toBeTruthy();
  });
});
